# for clustered proxy test
FPNN.fpzk.servers = localhost:5000,localhost:7000,localhost:6000
FPNN.fpzk.project = rtm
FPNN.fpzk.project_token = passwd
FPNN.fpzk.service_name = rtmSrv
# for non-cluster proxy test
#FPNN.proxy.serverEndpoints = localhost:3457, localhost:3458, localhost:3459, localhost:3450

# proxyType: 1 carpProxy, 2 rotatoryProxy, 3 randomProxy, 4 consistencyProxy
FPNN.proxy.type = 1
FPNN.proxy.sync = false
FPNN.proxy.duplex = true
FPNN.proxy.duplex.shared = true
FPNN.test.threadNum = 1
FPNN.test.sendCount = 10000

# for consistencyProxy
# all, half, one
FPNN.proxy.consistencyProxy.successCondition = half

FPNN.client.quest.timeout = 600
FPNN.server.idle.timeout = 600
