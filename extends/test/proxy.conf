# for non-cluster proxy test
FPNN.proxy.serverEndpoints = localhost:3457, localhost:3458, localhost:3459, localhost:3450

# proxyType: 1 carpProxy, 2 rotatoryProxy, 3 randomProxy, 4 consistencyProxy
FPNN.proxy.type = 2
FPNN.proxy.sync = false
FPNN.proxy.duplex = true
FPNN.proxy.duplex.shared = true
FPNN.test.threadNum = 10
FPNN.test.sendCount = 20000

# for consistencyProxy
# all, half, one
FPNN.proxy.consistencyProxy.successCondition = 

FPNN.client.quest.timeout = 60
